{
  "name": "@repo/observability",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "OpenTelemetry instrumentation — exporter swappable; no vendor lock-in.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./log": "./src/log.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
