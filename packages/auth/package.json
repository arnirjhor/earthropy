{
  "name": "@repo/auth",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Email + password and magic-link authentication. Self-hosted, no corp dependency. Real impl lands in Phase A.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo *.tsbuildinfo dist"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.2"
  },
  "dependencies": {
    "@node-rs/argon2": "^2.0.2",
    "@repo/database": "workspace:*",
    "drizzle-orm": "^0.36.4"
  }
}
