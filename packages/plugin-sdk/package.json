{
  "name": "@repo/plugin-sdk",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Plugin SDK for Earthropy — corp-agnostic extension points via hooks and lifecycle management.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./types": "./src/types.ts",
    "./registry": "./src/registry.ts",
    "./hooks": "./src/hooks.ts",
    "./lifecycle": "./src/lifecycle.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf .turbo *.tsbuildinfo dist"
  },
  "dependencies": {
    "@repo/observability": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.2",
    "vitest": "^3.2.2"
  }
}
