{
  "name": "@repo/design-system",
  "version": "0.0.1",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "description": "Earthropy design system — Tailwind tokens, shadcn primitives, SDG iconography. UI lands after a design pass.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./tokens": "./src/tokens.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf .turbo *.tsbuildinfo dist"
  },
  "dependencies": {
    "@repo/sdg": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
